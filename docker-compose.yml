#  C:\Users\benit\Downloads\Tienda-Online-main\BackendDj\docker-compose.yml
version: '3.8'

services:
  db:
    image: postgres:13
    volumes:
      - postgres_data:/var/lib/postgresql/data
    environment:
      POSTGRES_DB: railway
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ERMDtLlspadbrHFCEySgYIaXOJzQrjvO
    networks:
      - backend_network

  web:
    build: .
    command: ["gunicorn", "main.wsgi:application", "--bind", "0.0.0.0:8000"]
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=postgres://postgres:ERMDtLlspadbrHFCEySgYIaXOJzQrjvO@db:5432/railway
    depends_on:
      - db
    networks:
      - backend_network

volumes:
  postgres_data:

networks:
  backend_network:
